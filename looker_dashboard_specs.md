# Looker Studio Dashboard Configuration Specifications

**Generated by**: @bi-analyst  
**Main Data Source**: `victory_gold` dataset views  
**Purpose**: Basic demographics and leadership reporting  
**Last Updated**: 2026-02-22

---

## Dashboard 1: Demographics by Gender

### Data Source Setup
- **Connection Type**: BigQuery native connector
- **Table**: `victory_gold.vw_member_demographics`
- **No custom SQL** (connect directly to table)

### Chart Configuration
- **Chart Type**: Pie Chart
- **Dimension**: `gender`
- **Metric**: Record Count (or `COUNT(email)`)
- **Sort**: Descending by count
- **Style**:
  - Show data labels: Yes
  - Show legend: Right side
  - Colors: Use default palette or custom brand colors

### Filters
- None (show all members)

---

## Dashboard 2: Demographics by Age Group

### Data Source Setup
- **Connection Type**: BigQuery native connector
- **Table**: `victory_gold.vw_member_demographics`
- **No custom SQL**

### Chart Configuration
- **Chart Type**: Bar Chart (Vertical)
- **Dimension**: `age_group`
- **Metric**: Record Count (or `COUNT(email)`)
- **Sort**: Manual sort order:
  1. Kids
  2. Youth
  3. Young Adult
  4. Adult
  5. Senior
- **Style**:
  - Show data labels: Yes
  - Show grid lines: Yes
  - Colors: Gradient or single color

### Filters
- None (show all members)

---

## Dashboard 3: Demographics by Marital Status

### Data Source Setup
- **Connection Type**: BigQuery native connector
- **Table**: `victory_gold.vw_member_demographics`
- **No custom SQL**

### Chart Configuration
- **Chart Type**: Pie Chart or Donut Chart
- **Dimension**: `civil_status`
- **Metric**: Record Count (or `COUNT(email)`)
- **Sort**: Descending by count
- **Style**:
  - Show data labels: Yes
  - Show legend: Right side
  - Colors: Use default palette

### Filters
- None (show all members)

---

## Dashboard 4: VG Leaders and People Led

### Data Source Setup
- **Connection Type**: BigQuery native connector
- **Table**: `victory_gold.vw_victory_group_summary`
- **No custom SQL**

### Chart Configuration Part A: Leaders Table
- **Chart Type**: Table
- **Dimensions**: 
  - `first_name`
  - `last_name`
  - `vg_count` (# of people led)
- **Sort**: Descending by `vg_count`
- **Style**:
  - Show pagination: Yes (if more than 10 leaders)
  - Show row numbers: Optional

### Chart Configuration Part B: Summary Scorecards
Create 2 scorecards using the same filtered data source:

**Scorecard 1: Total VG Leaders**
- **Metric**: Record Count (or `COUNT(email)`)
- **Label**: "Total VG Leaders"
- **Style**: Large font, centered

**Scorecard 2: Total People Led**
- **Metric**: `SUM(vg_count)`
- **Label**: "Total People Led"
- **Style**: Large font, centered

### Layout
- Place scorecards at top
- Place leaders table below

---

## Dashboard 5: Occupation Breakdown

### Data Source Setup
- **Connection Type**: BigQuery native connector
- **Table**: `victory_gold.vw_member_demographics`
- **No custom SQL**

### Chart Configuration
- **Chart Type**: Bar Chart (Horizontal)
- **Dimension**: `employment_type`
- **Metric**: Record Count (or `COUNT(email)`)
- **Sort**: Descending by count
- **Style**:
  - Show data labels: Yes
  - Show grid lines: Yes
  - Highlight: Optional - highlight "Student", "Professional", "Business Owner" in custom colors

### Filters
- None (show all occupation types)

---

## General Best Practices

1. **Caching**: Looker Studio will cache results automatically. No custom SQL needed.
2. **Performance**: All queries hit the underlying views in `victory_gold` which are optimized for cost efficiency.
3. **Refresh**: Set automatic refresh to daily or on-demand only to minimize BigQuery costs.
4. **Sharing**: Configure appropriate access controls in Looker Studio settings.

---

## Implementation Steps for User

1. Open [Looker Studio](https://lookerstudio.google.com/)
2. Click "Create" → "Data Source"
3. Select "BigQuery" connector
4. Navigate to your project → `victory_gold` dataset
5. Select the relevant view (e.g., `vw_member_demographics` or `vw_victory_group_summary`)
6. Click "Connect"
7. Create a new report and repeat for other required views
8. Add charts following the specifications above
9. Arrange charts in a logical layout
10. Share with authorized users

---

## Future Enhancements

If additional dimensions are needed in the future, consult @data-engineer to add them to the relevant `.sqlx` files (e.g., `gold_demographics.sqlx`, `gold_vg_summary.sqlx`) in the Gold layer. Avoid creating custom SQL queries in Looker Studio to maintain caching and cost efficiency.
